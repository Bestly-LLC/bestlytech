

# Redeploy `run-pattern-maintenance` Edge Function

Single action: redeploy the `run-pattern-maintenance` Edge Function so it picks up the redesigned Bestly alert email template from `_shared/email-template.ts`.

No code changes needed — just a deploy.

